{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AiresView list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Commentaires</th>
                <th>Geom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('airesview_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.commentaires }}</td>
                <td>{{ entity.geom }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('airesview_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('airesview_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('airesview_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
