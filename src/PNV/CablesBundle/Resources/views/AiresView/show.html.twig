{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AiresView</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Commentaires</th>
                <td>{{ entity.commentaires }}</td>
            </tr>
            <tr>
                <th>Geom</th>
                <td>{{ entity.geom }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('airesview') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('airesview_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
