{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TInventaireTronconsErdf</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Remarques</th>
                <td>{{ entity.remarques }}</td>
            </tr>
            <tr>
                <th>Risque</th>
                <td>{{ entity.risque_integration_bati }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tinventairetronconserdf') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tinventairetronconserdf_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
